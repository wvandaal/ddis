{:tx/id #hlc "1774299671/0/af86f4b0b23bbbe4de81fe20af03f998"
 :tx/agent "af86f4b0b23bbbe4de81fe20af03f998"
 :tx/provenance :observed
 :tx/rationale "Set t-c76b7e9b title=ACP-TEST: ACP unification test suite. UNIT TESTS: (1) novelty_from_count algebraic properties — identity (N=0->1.0), monotonic decrease, half-life at N=4, approaches 0. (2) record_block_presentations retraction correctness — after 3 increments, only 1 Assert datom exists per attribute (old values retracted). (3) ContextBlock::new_scored defaults — System precedence -> impact 1.0, Methodology -> 0.8, Speculative -> 0.4. (4) Calibration cache hit — compute_calibration_metrics called once per command even when routing and context both need it. PROPTEST: (5) For any sequence of block presentations, the final count equals the number of presentations (no drift from retraction bugs). (6) For any set of context blocks with scores, project() output order matches precedence-then-score ordering. E2E SCRIPT (scripts/e2e_acp_unification.sh): (7) braid observe + braid status 3x — verify presentation counts increment (grep for count values in verbose output). (8) All 16 ACP commands produce _acp in JSON output. (9) No command produces attention:None in JSON (all blocks scored after ACP-SCORE-1). (10) Legacy footer path is unreachable (after ACP-LEGACY-1). FILE: crates/braid-kernel/tests/acp_unification.rs + scripts/e2e_acp_unification.sh"
 :tx/causal-predecessors []
 :datoms [
   {:e #blake3 "9d3a96654ead0df7e7d6feeddd711db919e396eb60e4567639ea9531016e96ba" :a :task/title :v "ACP-TEST: ACP unification test suite. UNIT TESTS: (1) novelty_from_count algebraic properties — identity (N=0->1.0), monotonic decrease, half-life at N=4, approaches 0. (2) record_block_presentations retraction correctness — after 3 increments, only 1 Assert datom exists per attribute (old values retracted). (3) ContextBlock::new_scored defaults — System precedence -> impact 1.0, Methodology -> 0.8, Speculative -> 0.4. (4) Calibration cache hit — compute_calibration_metrics called once per command even when routing and context both need it. PROPTEST: (5) For any sequence of block presentations, the final count equals the number of presentations (no drift from retraction bugs). (6) For any set of context blocks with scores, project() output order matches precedence-then-score ordering. E2E SCRIPT (scripts/e2e_acp_unification.sh): (7) braid observe + braid status 3x — verify presentation counts increment (grep for count values in verbose output). (8) All 16 ACP commands produce _acp in JSON output. (9) No command produces attention:None in JSON (all blocks scored after ACP-SCORE-1). (10) Legacy footer path is unreachable (after ACP-LEGACY-1). FILE: crates/braid-kernel/tests/acp_unification.rs + scripts/e2e_acp_unification.sh" :op :assert}
 ]}
