{:tx/id #hlc "1774306350/0/af86f4b0b23bbbe4de81fe20af03f998"
 :tx/agent "af86f4b0b23bbbe4de81fe20af03f998"
 :tx/provenance :observed
 :tx/rationale "Set t-b6350982 title=PERF-2: StatusSnapshot + RoutingResult disk cache. REVISED DIAGNOSIS: Store loading is fast (1.4s from binary cache, already implemented). The 30s bottleneck is compute_routing_from_store (O(tasks*datoms) with 256 tasks * 70K datoms) called 2-3x per status: once in build_status_projection via compute_action_from_store, once in build_json via derive_actions, once in build_verbose. APPROACH: (1) Compute RoutingResult (routings + calibration) ONCE per command, cache to .braid/.cache/routing.bin with same txn_fingerprint invalidation. (2) Cache StatusSnapshot (fitness, coherence, methodology_score, task_counts) to .braid/.cache/snapshot.bin — already partially implemented via fitness.json/coherence.json but not the full snapshot. (3) In status.rs::run(), check routing cache first. If fresh, deserialize. If stale, compute once and cache. (4) Pass cached routing to build_status_projection, build_json, and build_verbose instead of recomputing. (5) The routing cache is valid until a new txn file appears (same invalidation as store.bin). ACCEPTANCE: (A) status with warm routing cache: <2s. (B) status after new txn: recompute once + re-cache, then fast. (C) Second status call without new txns: <1s (pure cache deserialization). (D) Cache contents identical to fresh computation. (E) Routing called exactly once per command invocation. FILE: crates/braid/src/layout.rs + crates/braid/src/commands/status.rs + crates/braid-kernel/src/routing.rs"
 :tx/causal-predecessors []
 :datoms [
   {:e #blake3 "2210db08a0972b18f34704912bdc19dd11b42947acdc340d4bb9c3b40d3da71b" :a :task/title :v "PERF-2: StatusSnapshot + RoutingResult disk cache. REVISED DIAGNOSIS: Store loading is fast (1.4s from binary cache, already implemented). The 30s bottleneck is compute_routing_from_store (O(tasks*datoms) with 256 tasks * 70K datoms) called 2-3x per status: once in build_status_projection via compute_action_from_store, once in build_json via derive_actions, once in build_verbose. APPROACH: (1) Compute RoutingResult (routings + calibration) ONCE per command, cache to .braid/.cache/routing.bin with same txn_fingerprint invalidation. (2) Cache StatusSnapshot (fitness, coherence, methodology_score, task_counts) to .braid/.cache/snapshot.bin — already partially implemented via fitness.json/coherence.json but not the full snapshot. (3) In status.rs::run(), check routing cache first. If fresh, deserialize. If stale, compute once and cache. (4) Pass cached routing to build_status_projection, build_json, and build_verbose instead of recomputing. (5) The routing cache is valid until a new txn file appears (same invalidation as store.bin). ACCEPTANCE: (A) status with warm routing cache: <2s. (B) status after new txn: recompute once + re-cache, then fast. (C) Second status call without new txns: <1s (pure cache deserialization). (D) Cache contents identical to fresh computation. (E) Routing called exactly once per command invocation. FILE: crates/braid/src/layout.rs + crates/braid/src/commands/status.rs + crates/braid-kernel/src/routing.rs" :op :assert}
 ]}
