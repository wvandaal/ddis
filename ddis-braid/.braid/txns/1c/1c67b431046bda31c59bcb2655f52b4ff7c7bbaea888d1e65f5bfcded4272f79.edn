{:tx/id #hlc "1774299767/0/af86f4b0b23bbbe4de81fe20af03f998"
 :tx/agent "af86f4b0b23bbbe4de81fe20af03f998"
 :tx/provenance :observed
 :tx/rationale "Set t-00c2f4b2 title=EXT-BUG-2: braid status --json outputs plain text, not JSON. The --json flag is parsed but the status command always renders human-readable text. BACKGROUND: Other commands (query, task list) use CommandOutput.json field and --format json to produce structured output. Status builds an ACP ActionProjection and renders via project() which produces text. FIX: (1) In status.rs, check args.json or args.format==json. (2) When JSON requested, build a serde_json::Value with structured fields: store (datoms, entities, txns), coherence (fs, phi, b1, mt), boundaries (array of {name, score, gaps}), session (tasks, datoms, tx_since_harvest), hypotheses (array), actions (array from R(t)). (3) Include _acp metadata (budget_used, blocks_rendered, blocks_omitted). (4) Return via CommandOutput { json: structured_value, text: String::new() } so the output layer serializes JSON. ACCEPTANCE: (A) braid status --json produces valid JSON (parseable by jq). (B) JSON includes at minimum: store, coherence, boundaries, session, actions. (C) braid status (no flag) still produces human text. (D) braid status --format agent produces ACP text (existing behavior). FILE: crates/braid/src/commands/status.rs"
 :tx/causal-predecessors []
 :datoms [
   {:e #blake3 "9eee85b156055d105bb45e3c206e1457ff4bf043b2651ba12d9e5d2e038340ec" :a :task/title :v "EXT-BUG-2: braid status --json outputs plain text, not JSON. The --json flag is parsed but the status command always renders human-readable text. BACKGROUND: Other commands (query, task list) use CommandOutput.json field and --format json to produce structured output. Status builds an ACP ActionProjection and renders via project() which produces text. FIX: (1) In status.rs, check args.json or args.format==json. (2) When JSON requested, build a serde_json::Value with structured fields: store (datoms, entities, txns), coherence (fs, phi, b1, mt), boundaries (array of {name, score, gaps}), session (tasks, datoms, tx_since_harvest), hypotheses (array), actions (array from R(t)). (3) Include _acp metadata (budget_used, blocks_rendered, blocks_omitted). (4) Return via CommandOutput { json: structured_value, text: String::new() } so the output layer serializes JSON. ACCEPTANCE: (A) braid status --json produces valid JSON (parseable by jq). (B) JSON includes at minimum: store, coherence, boundaries, session, actions. (C) braid status (no flag) still produces human text. (D) braid status --format agent produces ACP text (existing behavior). FILE: crates/braid/src/commands/status.rs" :op :assert}
 ]}
