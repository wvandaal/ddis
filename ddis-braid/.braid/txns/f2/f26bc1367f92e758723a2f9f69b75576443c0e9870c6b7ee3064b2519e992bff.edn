{:tx/id #hlc "1773954751/0/af86f4b0b23bbbe4de81fe20af03f998"
 :tx/agent "af86f4b0b23bbbe4de81fe20af03f998"
 :tx/provenance :observed
 :tx/rationale "Set t-78bc87d7 title=RFL-2: Record projected action centrally in main.rs after run() — the PREDICTION half (INV-GUIDANCE-010). AUDIT FIX: Centralized in main.rs instead of scattered across command files. After run() returns, check if output.json contains _acp field. If so, extract action command/entity/impact/features from the _acp JSON and auto-transact as :action/* datom. Uses exit_warn_path (already extracted before run()) for store access. One recording point, not N per-command duplications."
 :tx/causal-predecessors []
 :datoms [
   {:e #blake3 "7f5c7918c450659b5b4abe998991e914a841a5f35f369ccd9a0219415db68a18" :a :task/title :v "RFL-2: Record projected action centrally in main.rs after run() — the PREDICTION half (INV-GUIDANCE-010). AUDIT FIX: Centralized in main.rs instead of scattered across command files. After run() returns, check if output.json contains _acp field. If so, extract action command/entity/impact/features from the _acp JSON and auto-transact as :action/* datom. Uses exit_warn_path (already extracted before run()) for store access. One recording point, not N per-command duplications." :op :assert}
 ]}
