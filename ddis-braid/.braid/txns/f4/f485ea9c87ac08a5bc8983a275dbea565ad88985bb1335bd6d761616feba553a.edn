{:tx/id #hlc "1774299703/0/af86f4b0b23bbbe4de81fe20af03f998"
 :tx/agent "af86f4b0b23bbbe4de81fe20af03f998"
 :tx/provenance :observed
 :tx/rationale "Set t-d95a1ab9 title=ATT-4-IMPL: Wire composite attention into ACP rendering pipeline — replace static precedence sort with UAQ-scored sort. BACKGROUND: ActionProjection::project() currently sorts blocks by OutputPrecedence enum ordinal, then by AcquisitionScore.composite() within tiers. After ACP-SCORE-1 (all blocks scored), the precedence enum becomes a COARSE tier while AcquisitionScore provides FINE ordering. The wire: (1) In project() (budget.rs), the sort already uses precedence-then-composite. Verify this path works when ALL blocks have scores (no None). (2) In format_footer (guidance.rs), the methodology context blocks are already scored. Verify scores flow through to the rendered output. (3) In maybe_inject_footer (mod.rs), ACP path preserves scores in JSON output (_acp.context_blocks[].attention). (4) Add a= score annotation to human-readable output for each visible block. ACCEPTANCE: (A) All rendered blocks show a= score in human output. (B) JSON output includes attention object for every block. (C) Higher-scored blocks appear before lower-scored blocks within same precedence. (D) Precedence still overrides score across tiers (System > Methodology always). DEPENDS-ON: ACP-SCORE-1 (t-238ea707). TRACES: INV-ATTENTION-005, ADR-FOUNDATION-024. FILE: crates/braid-kernel/src/budget.rs + crates/braid/src/commands/mod.rs"
 :tx/causal-predecessors []
 :datoms [
   {:e #blake3 "160a4474913524bad78e14e3d5a22ec940f8d175096cdb0a9cd314677076eb2c" :a :task/title :v "ATT-4-IMPL: Wire composite attention into ACP rendering pipeline — replace static precedence sort with UAQ-scored sort. BACKGROUND: ActionProjection::project() currently sorts blocks by OutputPrecedence enum ordinal, then by AcquisitionScore.composite() within tiers. After ACP-SCORE-1 (all blocks scored), the precedence enum becomes a COARSE tier while AcquisitionScore provides FINE ordering. The wire: (1) In project() (budget.rs), the sort already uses precedence-then-composite. Verify this path works when ALL blocks have scores (no None). (2) In format_footer (guidance.rs), the methodology context blocks are already scored. Verify scores flow through to the rendered output. (3) In maybe_inject_footer (mod.rs), ACP path preserves scores in JSON output (_acp.context_blocks[].attention). (4) Add a= score annotation to human-readable output for each visible block. ACCEPTANCE: (A) All rendered blocks show a= score in human output. (B) JSON output includes attention object for every block. (C) Higher-scored blocks appear before lower-scored blocks within same precedence. (D) Precedence still overrides score across tiers (System > Methodology always). DEPENDS-ON: ACP-SCORE-1 (t-238ea707). TRACES: INV-ATTENTION-005, ADR-FOUNDATION-024. FILE: crates/braid-kernel/src/budget.rs + crates/braid/src/commands/mod.rs" :op :assert}
 ]}
