{:tx/id #hlc "1773882474/0/af86f4b0b23bbbe4de81fe20af03f998"
 :tx/agent "af86f4b0b23bbbe4de81fe20af03f998"
 :tx/provenance :observed
 :tx/rationale "Set t-a4865988 title=PSR-1: Implement spec_relevance_scan with cosine bag-of-words scoring (INV-GUIDANCE-007). BACKGROUND: REVISED from TF-IDF to cosine similarity on bag-of-words. Spec statements are short (20-50 words) — TF-IDF works poorly on short text. Cosine on word bags is simpler, faster, and well-understood. ALGORITHM: (1) Tokenize input: lowercase, split whitespace, filter stopwords + words < 4 chars. (2) For each :spec/statement datom: same tokenization (can cache). (3) Score = |input_words ∩ spec_words| / sqrt(|input_words| × |spec_words|). (4) Also boost if input contains spec namespace name (e.g., 'topology' → TOPOLOGY namespace gets +0.3). (5) Return top 5 with score > 0.3 (REVISED: was 0.1, raised to avoid noise). Must complete in < 10ms on 400+ specs. TRACES TO: INV-GUIDANCE-007, C5. FILE: crates/braid-kernel/src/guidance.rs. ACCEPTANCE: scan('coupling matrix coordination') on store with topology spec returns INV-TOPOLOGY-004 with score > 0.3."
 :tx/causal-predecessors []
 :datoms [
   {:e #blake3 "7b2b9e127a9a730613396dde51088099133c2f8fa9470a288b2114c73b5010d4" :a :task/title :v "PSR-1: Implement spec_relevance_scan with cosine bag-of-words scoring (INV-GUIDANCE-007). BACKGROUND: REVISED from TF-IDF to cosine similarity on bag-of-words. Spec statements are short (20-50 words) — TF-IDF works poorly on short text. Cosine on word bags is simpler, faster, and well-understood. ALGORITHM: (1) Tokenize input: lowercase, split whitespace, filter stopwords + words < 4 chars. (2) For each :spec/statement datom: same tokenization (can cache). (3) Score = |input_words ∩ spec_words| / sqrt(|input_words| × |spec_words|). (4) Also boost if input contains spec namespace name (e.g., 'topology' → TOPOLOGY namespace gets +0.3). (5) Return top 5 with score > 0.3 (REVISED: was 0.1, raised to avoid noise). Must complete in < 10ms on 400+ specs. TRACES TO: INV-GUIDANCE-007, C5. FILE: crates/braid-kernel/src/guidance.rs. ACCEPTANCE: scan('coupling matrix coordination') on store with topology spec returns INV-TOPOLOGY-004 with score > 0.3." :op :assert}
 ]}
