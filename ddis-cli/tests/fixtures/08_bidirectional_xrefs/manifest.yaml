ddis_version: "3.0"
spec_name: "Bidirectional Cross-References"
tier_mode: "two-tier"

context_budget:
  target_lines: 4000
  hard_ceiling_lines: 5000
  reasoning_reserve: 0.25

constitution:
  system: "constitution/system.md"

namespaces:
  CMP:
    constitution: "constitution/cmp-system.md"
    description: "CMP namespace with bidirectional refs"

modules:
  main:
    file: "modules/main.md"
    domain: main
    maintains:
      - APP-INV-001
    interfaces: [CMP-INV-010]
    implements: []
    adjacent: []
    deep_context: null
    negative_specs: []

invariant_registry:
  APP-INV-001: { owner: main, domain: main, description: "References CMP, referenced by CMP" }
